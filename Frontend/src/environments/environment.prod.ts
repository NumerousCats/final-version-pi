export const environment = {
  production: true,
  apiUrl: 'http://localhost:8084', // Gateway URL
  services: {
    auth: '/api/auth',
    rides: '/api/rides',
    bookings: '/api/bookings',
    reviews: '/api/reviews',
    reports: '/api/reports'
  }
};

